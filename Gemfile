source 'https://rubygems.org'


# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.1.7'
# Use Postgress as the database for Active Record
gem 'pg'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.3'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer',  platforms: :ruby

# Use devise for users
gem 'devise'

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0',          group: :doc

# Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
gem 'spring',        group: :development

# Use Database Cleaner for fast truncate of the data an reset de PKs to 1
gem 'database_cleaner',        group: :development

# Use oEmbed resources
gem 'ruby-oembed'

# Admin panel
gem 'activeadmin', github: 'activeadmin'

# Feature flips
gem "flip"

# Add delay method
gem "delayed_job"

group :development, :test do
   gem 'rspec-rails'          # Use RSpec for testing
   gem 'mocha'                # Use Mocha for mocks
   gem 'shoulda-matchers'
   gem 'factory_girl_rails'   # Use FactoryGils for factories
end

group :test do
  gem 'faker'                 # Use Faker for generate fake data
  gem 'capybara'              # Use Capybara for simulate the user’s interactions
  gem 'guard-rspec'           # Use Gard-RSpec for watches the application and tests and runs specs for you automatically when it detects changes.
  gem 'launchy'               # Use Launchy for opens the default web browser upon failed integration specs to show you what your application is rendering
end

# Fix the jquery onload event handling that turbolinks breaks
gem 'jquery-turbolinks'

# Detect devise type and add header X_MOBILE_DEVICE
gem 'rack-mobile-detect'

# Use Twitter Bootstrap
gem 'bootstrap-sass',  '~> 3.3.1'

# Use Autoprefixer to add vendor prefixes to css
gem 'autoprefixer-rails'

# Use Font Awesome
gem 'font-awesome-sass'

# Webpay
gem 'webpay', '~>3.2'

# datepicker
gem 'pikaday-gem', '~> 1.2.0'

# momentjs for formatting dates
gem 'momentjs-rails', '~> 2.9.0'

# timepicker
gem 'jquery-timepicker-rails'

# internationalization
gem "i18n-js", ">= 3.0.0.rc8"

gem "sqlite3"

# Use unicorn as the app server
gem 'unicorn'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

